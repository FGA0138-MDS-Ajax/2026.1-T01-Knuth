# Copie para .env.local apenas se precisar sobrescrever a porta ou o protocolo.
# O host é ajustado automaticamente para coincidir com o frontend (localhost ou 127.0.0.1).
#
# VITE_API_URL=http://localhost:8000
